import * as React from "react";

/**
 * Brand a sheep currently in the field - colour them green - Assign newSheep with new property
 * Logic - sift through sheep 'objects' select gender, each time a male and female is selected, breed with 50% probability
 */

/**
 * Create function to iterate through Sheep
 */
